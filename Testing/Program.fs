// Learn more about F# at http://fsharp.org

open System
open KeySet

[<EntryPoint>]
let main argv =
    printfn "Hello World from F#!"

    //let a = Set.ofList [0 .. 5]
    //let ak = KeySet(a)

    //let b = Set.ofList [1..4]
    //let bk = KeySet(b)

    //let x = KeySet.union ak bk

    //printfn "%A" x.Values

    //printfn "Press ENTER to close..."
    //Console.ReadKey () |> ignore
    0 // return an integer exit code
